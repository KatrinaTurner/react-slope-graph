export interface SlopeGraphOptions {
  numLines: number;
  leftHeader: string;
  rightHeader: string;
  hoverColor: string;
}

export interface SlopeGraphFieldConfig {
  colorPalette: string;
  invertPalette: boolean;
}
